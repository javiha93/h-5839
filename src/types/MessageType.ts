
export interface MessageType {
  id: string;
  name: string;
}

export interface PatientInfo {
  code: string;
  firstName: string;
  middleName: string;
  lastName: string;
  sex: string;
  dateOfBirth: string;
}
